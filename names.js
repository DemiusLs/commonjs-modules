/**
 * Date due stringe nome e cognome, restituisce un oggetto con gli elementi
 * 
 * @param {string} firstName 
 * @param {string} lastName 
 * @returns 
 */
const retNameObj = (firstName , lastName) => ({firstName , lastName})

export default retNameObj;